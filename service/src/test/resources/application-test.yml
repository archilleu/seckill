spring:
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/miaosa?serverTimezone=UTC&allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8&useSSL=false

#日志
logging:
  path: logs/
  level:
    com:
      hoya:
        admin:
          dao: debug